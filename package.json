{
  "name": "mcp-unifi",
  "version": "0.1.0",
  "private": true,
  "description": "Remote MCP server for managing a fleet of UniFi consoles through the UniFi Site Manager and Cloud Connector proxy",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
